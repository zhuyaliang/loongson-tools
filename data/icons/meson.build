images = [
   ['loongson-fan.png'],
   ['loongson-firmware.png']
  ]
foreach image: images
  install_data(image,
    install_dir : join_paths(get_option('prefix'), 'share', 'loongson-tools', 'icons')
  )
endforeach
